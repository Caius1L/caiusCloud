spring:
  application:
    name: cloud-client
  profiles:
    active: dev
  cloud:
    config:
      uri: http://192.168.100.207:8080/
      label: dev
server:
  port: 7002

security:
  oauth2:
    resource:
      userInfoUri: http://localhost:20070/user
